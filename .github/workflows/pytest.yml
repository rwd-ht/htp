name: PyTest

on:
  push:
    branches:
      - dev
      - main
  # pull_request:
  #   types:
  #     - opened
  #     - synchronize
  workflow_dispatch:
jobs:
  PyTest:
    runs-on: ubuntu-latest
    # Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
    permissions:
      actions: read
      statuses: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - name: Setup PDM
        uses: pdm-project/setup-pdm@v4
        # You are now able to use PDM in your workflow
      - name: Install dependencies
        run: pdm install
      - name: Run tests
        run: pdm run coverage run -m pytest
        working-directory: .
      - id: save-coverage-html
        run: coverage html --title "Coverage for ${{ github.sha }}"
      - id: save-coverage-percentage
        run: |
          output=$(printf "%.02f" $(pdm run coverage json -q -o /dev/stdout | jq .totals.percent_covered))
          echo $output
          echo "coverage_percentage=$output" >> "$GITHUB_OUTPUT"
        working-directory: .
      - name: Store coverage files
        uses: actions/upload-artifact@v4
        with:
          name: coverage-html
          path: ./htmlcov
          include-hidden-files: true
      - name: Add commit status
        uses: octodemo-resources/github-commit-status@v1
        with:
          context: coverage
          state: success
          description: Coverage ${{ steps.save-coverage-percentage.outputs.coverage_percentage }}%
          target_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
